const handelHome = require('./handelHome');

const handelrequests = require('./handelrequests');

const htmllinks = require('./htmllinks');

const handleDetaled = require('./handleDetaled');

module.exports = {
  handelHome, handelrequests, htmllinks, handleDetaled,
};
